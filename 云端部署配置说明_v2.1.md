# 小学数学AI助教 · 云端部署配置说明（跨设备数据同步）

> 目标：让电脑、手机、平板登录同一账号，看到**同一份**班级 / 作业 / 学情数据。
> 方案：前端直连腾讯云 CloudBase（v1 经典版），用「匿名登录」满足数据库读写权限；
> 教师 / 学生 / 家长账号（手机号 + 密码）由本应用自行管理，存在云端 `sync` 集合里。
> 未配置环境 ID 时，网页自动退回「本地模式」，照常可用，不会报错。

---

## ⚠️ 一个前提说明（很重要）

CloudBase **新环境不支持「用户名 + 密码直接注册」**（官方限制，v1 / v2 都如此）。
所以本应用**不依赖 CloudBase 的账号体系**：

- 用 CloudBase「匿名登录」只为了拿到数据库读写权限（满足安全规则 `auth != null`）；
- 真正的师生家长账号（手机号 + 密码）全部存在云端 `sync` 集合里，由本应用管理，跨设备一致。

好处：您**只需要在控制台复制一个「环境 ID」**，不用碰 accessKey、不用短信验证、零额外成本。

> 安全提示：匿名登录意味着「拿到本应用链接的人」都能读写 `sync` 集合。
> 因此本方案适合**班级内部**（链接只发给学生 / 家长）使用；密码也存在该集合内。请勿把链接公开到互联网。

---

## 一、在代码里填入您的环境 ID（只需一次）

打开 `js/cloudbase.js`，找到文件顶部：

```js
/* ====== 👇 请在此填入您的 CloudBase 环境 ID ====== */
var ENV_ID = 'YOUR_CLOUDBASE_ENV_ID';
/* =================================================== */
```

把 `YOUR_CLOUDBASE_ENV_ID` 换成您在 CloudBase 控制台看到的环境 ID（形如 `math-ai-1gabcde123`）。
> 位置：CloudBase 控制台 → 环境 → 环境设置 → 复制「环境 ID」。

---

## 二、CloudBase 控制台准备（3 个小步骤）

1. **开通免费环境**：CloudBase 控制台新建环境，套餐选「免费体验版」（3000 资源点 / 月，长期免费，可续 6 个月）。地域任选（如上海 / 广州）。
2. **开启匿名登录**：`身份认证` → `登录方式`（或 `登录授权`）→ 打开 **匿名登录**。
   - 新环境默认可能关闭，请务必确认开关为「开」。这是本应用读写云端数据的必要条件。
3. **新建集合 + 安全规则**：
   - `数据库` → 新建集合，集合名填 **`sync`**（文档型数据库）。
   - 点 `sync` 集合 → `权限设置` / `安全规则` → 切换到「安全规则」模式，粘贴：

```json
{
  "read": "auth != null",
  "write": "auth != null"
}
```

> 含义：已登录（含匿名）用户可读写本集合。数据通过「班级 / 手机号」在本应用逻辑层隔离，适合师生家长熟人圈使用。
> 若控制台对某字段报错，以控制台提示为准；也可直接在界面选「所有用户可读，登录用户可写」。

---

## 三、本地预览（确认不报错）

在项目目录执行：

```bash
python -m http.server 8080
```

浏览器打开 `http://localhost:8080`。未填环境 ID 时是本地模式；填了环境 ID 且控制台配置正确，即为云端模式。

---

## 四、推送到公网（GitHub Pages）

```bash
git add -A
git commit -m "feat: 接入 CloudBase 跨设备同步"
git tag v2.1
git push origin main --tags
```

推送后访问 `https://zhaomath.github.io/math-ai/` 即可。GitHub Pages 通常 1–2 分钟生效。

---

## 五、日常使用流程（真实班级）

1. **教师**：打开网页 → 用手机号 + 自设密码 **注册**（账号写进云端 `users`）→ 创建班级 → **导入学生**（填姓名 + 手机号，系统给初始密码 `123456`）。
2. **学生 / 家长**：用**同一个手机号**登录（学生初始密码 `123456`，家长同理），首次登录即可使用；忘记密码可在登录页「密码重置」用手机号重设。
   - 跨设备：学生在电脑登录后，用手机登录同一手机号，数据自动同步（班级、作业、学情完全一致）。
3. **数据同步**：每次提交作业、教师批改、查看学情，都会后台同步到云端；换设备登录时自动拉取最新数据。

---

## 六、免费额度够不够（42 人班级）

- 免费体验版 = **3000 资源点 / 月**，长期免费、可续期。
- 本方案采用「集合级整体快照」同步：每次保存仅约 **5 次**数据库调用，42 人班级按教学节奏月调用约 **几百次**，远低于 3000 点上限，**富余约 3 倍**。
- 唯一限制：免费环境**不支持超量自动付费**，顶到上限会限流。正常使用不会触及；若将来带多个班 / 全校，升级「个人版」仅 19.9 元 / 月。

---

## 七、降级与排错

- **页面空白 / 一直本地模式**：检查 `js/cloudbase.js` 的 `ENV_ID` 是否已填、CDN 能否访问（`@cloudbase/js-sdk@1`）；并确认控制台已**开启匿名登录**。
- **数据不同步**：确认控制台 `sync` 集合已建、安全规则为 `auth != null`、匿名登录已开。
- **学生登录提示"未注册"**：学生手机号需由教师先在「导入学生」里加入；或学生自行用该手机号注册。
- **紧急回退**：把 `ENV_ID` 改回 `'YOUR_CLOUDBASE_ENV_ID'`，即整体退回本地模式，各设备本地数据仍在。

---

## 八、版本记录

- `v1.0`（git tag）：本地存储版，纯前端，数据仅在本机。
- `v2.0`（git tag）：首次接入 CloudBase（用户名密码方案，因新环境限制不可用，已被 v2.1 取代）。
- `v2.1`（git tag）：改用「匿名登录 + 应用自管账号」方案，仅需环境 ID 即可跨设备同步（本版）。
