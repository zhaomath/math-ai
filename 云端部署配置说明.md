# 小学数学AI助教 · 云端部署配置说明（跨设备数据同步）

> 目标：让电脑、手机、平板登录同一账号，看到**同一份**班级/作业/学情数据。
> 方案：前端直连腾讯云 CloudBase，本地仍保留缓存（断网可用），云端做数据同步。
> 未配置环境 ID 时，网页自动退回「本地模式」，照常可用，不会报错。

---

## 一、在代码里填入您的环境 ID（只需一次）

打开 `js/cloudbase.js`，找到文件顶部：

```js
/* ====== 👇 请在此填入您的 CloudBase 环境 ID ====== */
var ENV_ID = 'YOUR_CLOUDBASE_ENV_ID';
/* =================================================== */
```

把 `YOUR_CLOUDBASE_ENV_ID` 换成您在 CloudBase 控制台看到的环境 ID（形如 `math-ai-1gabcde123`）。
> 位置：CloudBase 控制台 → 环境 → 环境设置 → 复制「环境 ID」。

---

## 二、CloudBase 控制台准备（4 个小步骤）

1. **开通环境**：CloudBase 控制台新建环境，套餐选「免费体验版」（3000 资源点/月，长期免费，可续 6 个月）。
2. **开启登录方式**：`身份认证` → `登录方式` → 打开 **用户名密码登录**。
   - 若控制台要求用户名必须是邮箱格式，无需处理——本程序已自动把手机号规范为 `手机号@math.local` 作为用户名。
3. **新建集合**：`数据库` → 新建集合，集合名填 **`sync`**（文档型数据库）。
4. **设置安全规则**（点 `sync` 集合 → 权限设置 / 安全规则），粘贴下面内容：

```json
{
  "read": "auth != null",
  "write": "auth != null"
}
```

> 含义：已登录用户（匿名或用户名密码）可读写本集合。本应用数据通过「班级/手机号」在业务逻辑层隔离，适合师生家长熟人圈使用。
> 如控制台报错，以控制台给出的语法提示为准；也可直接在控制台界面选择「所有用户可读，登录用户可写」。

---

## 三、本地预览（确认不报错）

在项目目录执行（任选其一）：

```bash
python -m http.server 8080
# 或
npx serve .
```

浏览器打开 `http://localhost:8080`。未填环境 ID 时是本地模式；填了环境 ID 且控制台配置正确，即为云端模式。

---

## 四、推送到公网（GitHub Pages）

```bash
git add -A
git commit -m "feat: 接入 CloudBase 跨设备同步"
git tag v2.0
git push origin main --tags
```

推送后访问 `https://zhaomath.github.io/math-ai/` 即可。GitHub Pages 通常 1–2 分钟生效。

---

## 五、日常使用流程（真实班级）

1. **教师**：用手机号注册（即创建 CloudBase 账号）→ 创建班级 → **导入学生**（填学生手机号+姓名）。导入会把这些手机号写进云端 `users`。
2. **学生 / 家长**：用**同一个手机号**自行注册 CloudBase 账号（密码自设）→ 登录后即复用教师导入的身份，能看到自己的作业与学情。
   - 跨设备：学生在电脑注册登录后，用手机登录同一手机号，数据自动同步。
3. **数据同步**：每次提交作业、教师批改、查看学情，都会自动后台同步到云端；换设备登录时自动拉取最新数据。

---

## 六、免费额度够不够（42 人班级）

- 免费体验版 = **3000 资源点/月**，长期免费、可续期。
- 本方案采用「集合级整体快照」同步：每次保存仅约 **5 次**数据库调用，42 人班级按教学节奏月调用约 **几百次**，远低于 3000 点上限，**富余约 3 倍**。
- 唯一限制：免费环境**不支持超量自动付费**，顶到上限会限流。正常使用不会触及；若将来带多个班/全校，升级「个人版」仅 19.9 元/月。

---

## 七、降级与排错

- **页面空白 / 一直本地模式**：检查 `js/cloudbase.js` 的 `ENV_ID` 是否已填、CDN 是否能访问（`@cloudbase/js-sdk`）。
- **登录提示"未注册"**：学生/家长需先在 App 内用自己手机号注册 CloudBase 账号（教师已导入手机号即可复用身份）。
- **数据不同步**：确认控制台 `sync` 集合已建、安全规则为 `auth != null`、登录方式已开。
- **紧急回退**：把 `ENV_ID` 改回 `'YOUR_CLOUDBASE_ENV_ID'`，即整体退回本地模式，原数据仍在各设备本地。

---

## 八、版本记录

- `v1.0`（git tag）：本地存储版，纯前端，数据仅在本机。
- `v2.0`（git tag）：接入 CloudBase，实现跨设备数据同步（本版）。
